<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="match"
            type="com.example.kabaddikounter.data.Match" />
        <variable
            name="isSubscribed"
            type="boolean" />
        <!--
            statusColor dikirim sebagai Int (Color.parseColor)
            karena status bisa "LIVE", "END", atau nilai lain
        -->
        <variable
            name="statusColor"
            type="int" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:layout_marginVertical="6dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- "Tim A vs Tim B" — binding langsung dari variabel match -->
            <TextView
                android:id="@+id/textTeams"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@{(match.teamA.empty ? `Tim A` : match.teamA) + ` vs ` + (match.teamB.empty ? `Tim B` : match.teamB)}"
                android:textColor="@color/text_secondary"
                android:textSize="13sp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Skor -->
            <TextView
                android:id="@+id/textScore"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@{String.valueOf(match.scoreA) + ` — ` + String.valueOf(match.scoreB)}"
                android:textColor="@color/text_primary"
                android:textSize="36sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/textTeams" />

            <!-- Status: warna diset dari adapter via statusColor -->
            <TextView
                android:id="@+id/textStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@{match.status}"
                android:textColor="@{statusColor}"
                android:textSize="14sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/textScore" />

            <!--
                Tombol Subscribe / Unsubscribe
                - Text dan warna diatur via BindingAdapter (lihat MatchBindingAdapters.kt)
                - visibility diatur di Adapter (LIVE = VISIBLE, END = GONE)
            -->
            <Button
                android:id="@+id/btnSubscribe"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="13sp"
                android:visibility="gone"
                app:subscribeState="@{isSubscribed}"
                app:cornerRadius="6dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>